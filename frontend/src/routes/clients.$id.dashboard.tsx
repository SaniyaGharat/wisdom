import { createFileRoute } from "@tanstack/react-router";
import { MatchDashboard } from "@/components/match-dashboard";
export const Route = createFileRoute("/clients/$id/dashboard")({ head: () => ({ meta: [{ title: "Client Matches — Matchleaf" }, { name: "description", content: "Review and manage ranked supplier matches." }, { property: "og:title", content: "Client Matches — Matchleaf" }, { property: "og:description", content: "Review and manage ranked supplier matches." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Page });
function Page() { const { id } = Route.useParams(); return <MatchDashboard kind="client" id={id} />; }